import styled from "@emotion/styled";
import { Button, IconButton } from "@mui/material";

export const TopMenuLoginButton = styled(Button)`
  color: white;
`;

export const TopMenuUserButton = styled(IconButton)`
  color: white;
`;

export const TopMenuResultsButton = styled(IconButton)`
  color: white;
`;
